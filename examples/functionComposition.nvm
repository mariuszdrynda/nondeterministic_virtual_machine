fn composition(f:(a:{b,String}), g:(b:{c,String})):(a:{c,String}){
    ret \\(x:a):{c,String}{
        p1 = f(x);
        p2 = g(p1[1]);
        ret p2[1], p1[2]+p2[2]
    }
};
fn main():Void{
    //TODO
}