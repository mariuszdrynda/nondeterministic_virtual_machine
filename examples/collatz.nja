fn collatz(i){
    println(i);
    if(i == 1){
        ret 1
    } else {
        if(i % 2 == 0){
            collatz(i / 2)
        } else {
            collatz(i * 3 + 1)
        }
    }
}

fn main(){
   collatz(int(getln()))
}